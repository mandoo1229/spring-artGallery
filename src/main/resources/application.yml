server:
  port: 8080

spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
  thymeleaf:
    cache: false


  h2:
    console:
      enabled: true

  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/art_gallery?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: Fngml2tp^^
    driver-class-name: com.mysql.cj.jdbc.Driver
# 파일 용량
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 10MB

  #    prefix: file:src/main/resources/templates

  jpa:
    #    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    show_sql: true
    hibernate:
      ddl-auto: create
    properties:
      format_sql: true
      use_sql_comments: true

# swaggerdoc
springdoc:
  #  packages-to-scan: com.encore.playground
  swagger-ui:
    path: /apiui.html
    tags-sorter: method
    operations-sorter: method
  api-docs:
    groups:
      enabled: true
    path: /api-docs/json
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  paths-to-match: /api/**

logging:
  file:
    path: /Users/real/Desktop/file/image/